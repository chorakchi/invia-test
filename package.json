{
  "name": "invia-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4",
    "express": "^4.16.2",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "parallelshell": "^3.0.2",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "server-start": "yarn run server-build && node dist/server/index.js",
    "build": "react-scripts build",
    "server-build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./README.md,./public,./src,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
